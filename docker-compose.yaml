---
services:
  comfyui-boot:
    image: comfyui:v0.33.1
    container_name: comfyui
    restart: unless-stopped
    stdin_open: true
    tty: true
    ports:
      - 8188:8188
    volumes:
      - ./data/user:/app/user:Z
      - ./data/input:/app/input:Z
      - ./data/output:/app/output:Z
      - ./data/models:/app/models:Z
      - ./data/custom_nodes:/app/custom_nodes:Z
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
